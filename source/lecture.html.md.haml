- # set the page title to the lecture title
- content_for(:title, lecture)

- # these instance variables inform the breadcrumb partials
- # weird, right? Totes works, though
- @unit, @chapter, @lecture = [unit, chapter, lecture]

-# We're reading the file on every render on purpose.
-# So we can dynamically reload changes to the markdown file
-# makes for easier editing.
.lecture= Kramdown::Document.new(File.read(file)).to_html
